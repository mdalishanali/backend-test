{
  "version": 2,
  "builds": [
    {
      "command": "npm run tsc",
      "use": "@vercel/node",
      "config": { "includeFiles": ["dist/**"] }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/lib/index.js"
    }
  ]
}
